import { reject, isNil } from 'ramda';
/**
 * Filters out every nil value in a list.
 *
 * @func
 * @category List
 *
 * @example
 *
 *        R_.rejectNil([null, undefined, '']); // ['']
 *
 * @sig [a] -> [a]
 *
 */

var rejectNil = /*#__PURE__*/reject(isNil);
export default rejectNil;